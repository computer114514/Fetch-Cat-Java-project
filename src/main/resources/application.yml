server:
  port: 8081
#  指定端口号

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cat_catcher?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 192.168.100.128
      port: 6379
      password: 123321
      database: 3
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

mybatis-plus:
  type-aliases-package: com.github.computer114514.domain.enity
  configuration:
    map-underscore-to-camel-case: true #开始驼峰
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #打印日记
  global-config:
    db-config:
      id-type: auto #全局--数据库配置--主键自增